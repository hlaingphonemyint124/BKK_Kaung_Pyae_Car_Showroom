import React from "react";
import SoldHistory from "../section/SoldHistory";

export default function SoldHistoryPage() {
  return (
    <div className="sold-history-page">
      <SoldHistory />
    </div>
  );
}